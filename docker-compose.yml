version: "3.9"

services:
    front:
        container_name: front
        build:
            context: .docker/development/node
        volumes:
            - ./:/app
        tty: true
        hostname: front
        ports:
            - "5173:5173"
        networks:
            - front

networks:
    front:
        name: front
